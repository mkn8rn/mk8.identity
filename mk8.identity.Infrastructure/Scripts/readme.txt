Import using:
    Import-Module .\mk8.identity.Infrastructure\Scripts\addmig.psm1

Usage:
    addmig id Initial      # IdentityContext
    addmig app Initial     # ApplicationContext
    updb id                # Update IdentityContext
    updb app               # Update ApplicationContext

Note: Run from Package Manager Console (PMC) in Visual Studio.
